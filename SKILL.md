---
name: moody-xhs-hot-content
description: moody美瞳小红书热点内容一体化创作。输入产品名（品牌/花色）、热点信息和一张参考图，自动完成：相关热点搜索与整理、moody在售产品库汇总（名称/直径/材质/花色/联名）、热点-产品匹配选品分析、20字内情绪化选题、300字内小红书爆款文案（含三类医疗器械合规尾注）、参考图拆解与产品图提示词生成、产品图生成。当用户给出热点信息+美瞳产品求小红书文案和配图、要求"热点结合产品出种草内容"、"给moody美瞳写小红书爆款"、提供参考图求同款风格产品图等场景时触发。
agent_created: true
---

# moody 美瞳小红书热点内容创作

## 概述

把【热点信息 × moody 产品 × 参考图】三要素加工成一套可直接发布的小红书种草内容包：热点整理 → 产品库汇总 → 选品结论 → 选题 → 爆款文案 → 参考图拆解 → 产品图提示词 → 产品图。

## 输入要素

开工前确认以下三项，缺一向用户询问，不要自行编造：

| 要素 | 说明 |
|---|---|
| 产品名 | 默认 moody 美瞳。若用户指定具体花色（如"胡桃棕"），仍完整执行产品库汇总，再验证该花色是否为热点最优解 |
| 热点信息 | 一句话 / 关键词 / 一段描述，如"韩系水光妆回潮"、"多巴胺穿搭" |
| 参考图 | 本地图片路径，用于拆解产品图风格 |

## 工作流程（8 步，严格按序执行，每步产出是下一步输入）

### 第 1 步 热点搜索与整理

用 WebSearch（2-4 组关键词）围绕热点信息搜索：热点本身的内容形式、小红书/抖音/微博上的热门相关内容、与眼妆美瞳的结合点。整理为热点清单表：`热点关键词 | 热度表现 | 与美瞳结合点 | 可借用情绪词`。只保留与美瞳内容强相关的 3-6 条，注明哪些关键词可用于第 5 步文案和第 7 步提示词。

### 第 2 步 moody 在售产品库汇总

用 WebSearch 汇总 moody 全部在售产品，每个花色必须覆盖：产品系列、花色名、镜片直径/着色直径、材质（硅水凝胶/水凝胶）、含水量、抛弃周期、联名 IP、花色一句话描述。目标 ≥15 个花色，联名款必须全部包含。搜索渠道优先级、搜索词模板与品牌基线知识见 `references/moody-product-knowledge.md`（执行本步前先读）。表格参数须与来源核对，查不到的标"未查证"，禁止编造。

### 第 3 步 热点-产品匹配分析

按四个维度给候选花色打分（1-5 分）：色调匹配、直径匹配、材质/场景匹配、联名热度加成。打分方法见 `references/moody-product-knowledge.md`。输出：Top1 推荐产品 + Top2 备选 + 一段推荐理由（必须引用热点关键词与产品卖点）。若用户指定了花色，用打分表验证并说明结论（可能确认，也可能建议更优解，把决定权交给用户）。

### 第 4 步 选题生成

基于热点与 Top1 产品产出 3 个候选选题，每个：≤20 字（含 emoji 与标点）、有情绪表达、带 1-2 个 emoji、拒绝生硬卖点词。规则与好坏例见 `references/xhs-copywriting.md`（执行本步前先读）。标注最推荐的 1 个并说明理由。

### 第 5 步 小红书正文创作

按 `references/xhs-copywriting.md` 的结构模板与风格规则写正文：≤300 字，结构为 情绪钩子开头 → 花色氛围段 → "-" → 产品标签行（#产品名 直径/材质/周期）→ "-" → 卖点段 → 合规尾注。合规尾注（医疗器械提示语 + 广审批文号 + 图片声明）必须原样保留：批文号使用所选产品的真实号码，搜索不到就向用户索要，禁止编造。

### 第 6 步 参考图拆解

用 Read 工具查看参考图（多模态识图），按 10 个维度输出拆解表：景别构图 / 机位角度 / 光线 / 色调 / 模特特征 / 妆造重点 / 发型发色 / 服装配饰 / 背景道具 / 氛围与后期风格。维度清单见 `references/image-prompt-guide.md`（执行本步前先读）。只拆解风格要素，不复刻可识别的人物长相（肖像权）。

### 第 7 步 产品图提示词生成

把拆解结果转成完整提示词（中文版 + 英文版），公式与模板见 `references/image-prompt-guide.md`。提示词必须植入所选产品的花色细节：锁边颜色、内圈渐变、着色直径效果、水光/高光质感，并贴合第 1 步整理的热点氛围词。

### 第 8 步 产品图生成与交付

用 ImageGen 生成产品图：先用 ToolSearch 加载 ImageGen 工具，再用 DeferExecuteTool 调用。生成前告知用户每张图约消耗 5-10 credits 并确认数量（默认 2-4 张变体）：

- 优先图生图：参考图作为 image1 输入 + input_fidelity 设为中低值 + 提示词描述"保留构图光线氛围、替换花色妆造为所选产品"
- 备选纯文生图：只用第 7 步提示词，从零生成
- 尺寸用 1024x1536（小红书 3:4 竖图）

生成后逐张自检：花色是否与所选产品一致（对照第 2 步产品表）、眼部是否自然无畸形（双眼对称、瞳孔位置、花色不溢出虹膜）、光泽感是否在线；不合格的重新生成。

最终交付：在对话中完整输出 热点整理表 → 产品库表格 → 选品结论 → 3 个候选选题 → 可直接复制发布的完整文案（含合规尾注）→ 参考图拆解表 → 中英文提示词，并用 present_files 展示产品图文件。可选：把文案与提示词保存为 md 文件一并交付。

## 红线（每条必须遵守）

1. 美瞳是第三类医疗器械：合规尾注句式不得删改；禁止医疗功效宣称（"治疗""矫正视力"）与绝对化用语（"最""第一""永久""100%"）
2. 广审批文号必须真实对应产品；查不到 → 向用户索要，绝不编造
3. 产品参数以搜索来源为准，不确定的标"未查证"
4. 参考图只取风格要素，不生成可识别的真人肖像
5. 字数硬指标：选题 ≤20 字、正文 ≤300 字（写完必须数字数）
6. 生成图片前必须告知用户 credit 消耗并确认

## 资源文件

| 文件 | 内容 | 何时读 |
|---|---|---|
| `references/moody-product-knowledge.md` | 品牌基线、产品线速查、搜索策略、产品表格格式、匹配打分法 | 第 2-3 步前 |
| `references/xhs-copywriting.md` | 选题规则与好坏例、正文结构模板、风格规则、合规尾注、完整示例 | 第 4-5 步前 |
| `references/image-prompt-guide.md` | 参考图拆解维度、提示词公式与中英模板、ImageGen 用法与自检清单 | 第 6-8 步前 |
